"""MiniCode Agent package.

The package root intentionally exposes only the version. Import concrete
runtime, model, tool, security, execution, persistence, evaluation, or Web
APIs from their dedicated bundle so dependency direction stays visible.
"""

__version__ = "0.1.0"
